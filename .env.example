# Recoup (Vasooli) Configuration Template
# Copy this file to '.env' and fill in your values. Do NOT commit the '.env' file.

# Google Gemini Flash API Key (Optional)
# If left blank, the application will degrade gracefully and run in mock fallback mode.
GEMINI_API_KEY=your_gemini_api_key_here

# Application configuration (Optional overrides)
# SERVER_PORT=8080
